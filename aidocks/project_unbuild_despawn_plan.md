---
name: project_unbuild_despawn_plan
description: Design plan for the unbuild and despawn commands (remove objects placed by build/spawn); NOT built
metadata:
  type: project
---

**STATUS: unbuild AND despawn BOTH BUILT & shipping in 14.8 (all 9 despawn sections + docs done).** unbuild = `unbuild_entity()` in mapfuncts.nvgt (read_map_main_sif → delinear → find line → remove_at → push_map_undo → write_map_main_sif → load_map) + the `ub`/`unbuild` branch in command_parser.nvgt + allcommands entry + commands.txt + changelog. Feedback used: map-update sound + "Map updated." on success, "There's no <type> to remove." on miss. despawn NOT started — it's the in-memory, trusted-on-compiled counterpart (per-type teardown, sentinel-null; composites call their existing cleanup like a wall's platform_ids loop).** Two commands that remove what `/build` and `/spawn` place — the entity-cleanup gap the spawn help text already calls out ("the only cleanup verbs are /kill and /killall, npc-only; a /spawn of a non-npc entity has no command to remove it"). See [[project_trusted_commands_compiled]] (gating) and the build/spawn branches in `command_parser.nvgt`.

**Names & aliases (settled):**
- **unbuild** (`ub`) — inverse of `/build`. Permanent: removes the entity's line from `main.sif` and reloads. Decompiled-only.
- **despawn** (`dsp`) — inverse of `/spawn`. In-memory: removes the live entity from the session. Trusted-on-compiled.
(Aliases verified free earlier.)

**Gating (settled):**
- **unbuild = decompiled-only** (`&& !map_is_compiled`), like `/build` — it edits `main.sif`, which doesn't exist in a compiled `.map`. Belongs with the editing verbs, NOT the trusted-bypass set.
- **despawn = trusted-on-compiled** (`&& (!map_is_compiled || trusted)`), exactly like `/spawn` — so a switch/sensor/input/timer that spawns-on / despawns-off works on a shipped map, while typing despawn in the console on a compiled map stays blocked.

**Removal semantics (settled — the dev's simplification, no retyping fields):**
- **Bare form `<cmd> <type>`** → removes the MOST-RECENTLY-ADDED entity of that type (LIFO). `despawn sign` drops the newest sign; repeat to walk back. This is the common case and matches the build/test workflow.
- **Optional coord form `<cmd> <type> <x> <y> [z]`** → removes the specific one at those coordinates (coords ONLY, never the full field list) — the precision escape hatch when several exist and you don't want the newest. z on 3d maps.
- Arg parsing distinguishes the two by token count + mapmode (2 tokens = type-only; type + 2 or 3 coords = coord form).
- **Coord match = PLACEMENT CORNER (SETTLED):** the coordinate you give is the entity's own placement corner — a single-tile entity's `x y[ z]`, or a ranged entity's `minx miny[ minz]` — matched EXACTLY (not containment). Multiple entities at different corners each get uniquely targeted; if two share the exact same corner, the NEWEST (last in file / LIFO) is removed, repeatable. Tiny false-match risk (a ranged line whose maxx coincides), documented, acceptable.
- Type name accepts friendly and on-disk forms via `normalize_buildtype`, same as build/spawn.

**Why it's safe (settled):**
- despawn is in-memory only → NON-DESTRUCTIVE: over-despawning (removing a map entity, not just a spawn) is undone by a map reload.
- unbuild edits the file but is RECOVERABLE via `/undomap` (`/ud`), which already covers add/edit/remove of map lines.

**Implementation approach (settled direction):**
- **despawn:** find the target in the appropriate entity array by type; bare = last non-null of that type (highest index — spawns append to the end, so this reliably gets the newest spawn); coord = the non-null match at those coords. Remove via SENTINEL-NULL (`@<entity>s[i] = null;` + guarded loops/derefs) per the entity-array stability rule — NOT resize/remove_at.
- **unbuild:** read `main.sif` lines, find the last line (or coord-matching line) whose keyword matches the type, delete that line, write back, reload (`load_map`), like build's write-and-reload but removing.

**Scope (SETTLED — ALL entity types, both commands):**
- Composite entities (walls, ranged platforms, staircases) own a bundle of sub-tiles (e.g. a 3d wall's `platform_ids`, one platform per z — wall.nvgt:35-36). Removal must drop the whole bundle.
- **unbuild gets composites for FREE:** it deletes the single main.sif line and reloads; the reload rebuilds the map without that entity, so all sub-tiles vanish with no per-tile bookkeeping. Covers every type day one.
- **despawn reuses each entity's EXISTING teardown:** simple entities (sign, sound_source, hazard, item) = null the array slot (sentinel-null); composites call their already-written cleanup (a wall's destroy loops `platform_ids` and calls `remove_platform` — wall.nvgt:101-102). Per-type wiring, but no new removal algorithms.

**DESPAWN ARCHITECTURE (SETTLED):** must be SURGICAL — remove one live in-memory entity without touching anything else. A reload-based approach (track spawns as lines, reload + re-apply survivors) is REJECTED: `load_map` with default `force_spawned=false` keeps the player's position (only true teleports to spawn — map_parser.nvgt:837), BUT a reload resets ALL other runtime state (toggled switches, moved/killed NPCs, opened doors, collected items, timers), which is unacceptable for a gameplay cleanup command fired by a switch on a shipped map. So despawn is a per-type removal dispatch, `despawn_entity(type, by_coord, gx, gy, gz)`, mirroring `dispatch_entity_line` (map_parser.nvgt:1) — each of ~70 types has its own array, coord fields, and PROPER teardown (stop sounds, drop composite sub-tiles via platform_ids, etc.); nulling a slot without correct teardown leaks sounds/tiles. Match = last non-null of type (bare) or coord match (placement corner), newest wins, sentinel-null removal per the stability rule.
**DESPAWN CRITICAL LESSON (from a Section-2 crash):** making a previously-COMPACTED array (one never nulled at runtime) nullable via despawn means EVERY loop over it must guard `if(@arr[i]==null)`. The entity's own file is not enough — three cross-cutting consumers bit us: `apply_effect_pools()` in effect_space.nvgt (audio, per-frame), `spier.nvgt` (tracking/info), and the array's own `destroy_all_<type>()` (map-change). For each newly-nullable array, GREP `<array>.length()` across the whole codebase and guard every site. Already-nullable arrays (sign/switch/clock/instrument/bomb/hazard/etc.) were fine — their consumers already guard because they use sentinel-null by design. NOTE for construction: platforms[] is ALREADY nullable via remove_platform (walls/hazards use it), so its many consumers already guard — reuse remove_platform for despawn_platform rather than nulling directly.
**DESPAWN FEEDBACK (SETTLED):** SILENT on success (gameplay cleanup, unlike unbuild's authoring "Map updated."); spoken notice only when nothing of that type exists to remove.
**DESPAWN BACKING-PLATFORM RULE (SETTLED):** any entity that spawns backing platform tiles into platforms[] must remove them on despawn. If the entity already tracks ids (wall/staircase platform_ids, moving_platform platform_id, hazard platid/platid2), loop/remove them. If it DISCARDS the ids (floor_breaker air tile, belt per-z tiles, vanishing_platform's re-spawned tile), ADD id storage (a field or platform_ids array), capture on every spawn_platform, and remove on despawn — done for floor_breaker (platid), belt (platform_ids), vanishing_platform (platid, which also fixed a per-cycle accumulation leak). CHECK remaining categories (transitions: elevator/lift/el_floor/passage/trampoline may lay platform tiles) for the same pattern.
**DESPAWN REMOVAL-SOUND RULE (SETTLED):** the "silent" rule is about SPOKEN feedback only. Where an entity has its OWN removal SOUND EFFECT (a `*remove*` clip or similar death/removal cue — typically played by its `destroy_all_<type>`), despawn should PLAY it at the entity's position, matching the normal teardown. Done for floor_breaker (plays its `*remove*` via breakerpool.play_3d). PENDING for future categories: HELPER NPCs have remove sounds (sounds/decompiled/builder/kombat/npc/helpers/*) — handle in the Kombat section; and check every entity's destroy path for a removal cue before finalizing its despawn.
**DESPAWN SECTIONS (SETTLED — one category at a time):** (1) [DONE] Foundation + Interaction category [branch gated `&& (!map_is_compiled || trusted)`, arg parsing, dispatch skeleton, shared match logic, feedback, allcommands, + sign/switch/sensor/clock/calendar/instrument/text_input/menu_input/text_square]; (2) [DONE] Traps (added null guards to mhazardcheck/vhazardcheck/windloop/forceloop/forcefield_check/destroy_all_floorbreakers; wrapper types remove_hazard their child; hazard drops ledge platids; floor_breaker now STORES its air-tile platid (was discarded) and removes it on despawn to restore the floor; camera 3 slots, forcefield 2 slots); (3) Construction — split into 3a [DONE: already-nullable platform/wall/staircase/slant; platform_is_wrapped skips composite-owned tiles; slant's leaking platform_ids loop added; onstaircase/player_slant reset] and 3b [DONE: compacted vanishing_platform/blockage/moving_platform/belt/spawnpoint; guards added across each entity's own loops + destroy_all + apply_effect_pools + spier + hook (loops & indexed follow) + sonar + load_map spawnpoint[0]; looping sounds vplatsound/platsound/beltsound stopped; platformmove + last_spawnpoint + onstaircase + player_slant resets; verified all .length() sites guarded]; (4) [DONE] Audio — sound_source/sound_ambience/speaker/timed_source/timed_ambience/url_source/url_ambience/excludable_source/excludable_ambience/effect_space; all use sorcesound+own pool (speaker=lsound/rsound in speakerpool/speakerpool2; effect_space no sound, fx self-heals); guards added to each per-frame update/loop, each destroy_all, and apply_effect_pools :112-127 + the effect_space scan :34; spier/sonar have no refs; speakers was already guarded; verified all .length() sites; (5) [DONE] Zones — all 13 (menu/safe/fall/text/obscurity/heal/tile/flux/item(objzones)/story/bomb/projectile/npc); no looping sounds (safe_zone plays *out* cue if player inside on despawn); no backing tiles; item/bomb/projectile/npc spawn UNTRACKED children — despawn stops spawning, leaves existing children; special coord fields: bomb_zone(minx/bomby/bombz), projectile_zone(minx/proy/proz), npc_zone(minx/y/z); guarded index-helpers (player_in_safezone/healzone/tilezone/fluxzone + cam_in_safezone) cover their loops; cross-cutting guards: effect_space healzones, spier safezones, map.nvgt+game_handlers safezones, command_parser text_zones, map_menu go-to-zone (added gozone_idx original-index tracking so nulls don't misalign the positional menu); arena pre-guarded; (6) [DONE] Transitions — door/passage/elevator/el_floor/lift/teleporter/travelpoint/trampoline; door+teleporter already nullable; passage removes its platform_ids tiles + stops sounds (coords startx/starty/z), lift stops always-loop liftsound + remove_platform(platform_id) + liftmove reset, elevator stops loop/beep/move + elvmove reset (coords startx/starty/startz), el_floor mode-dependent y (floory 2d / miny+z 3d), trampoline z-single; guards on passage/elevator/el_floor/lift/travelpoint loops + destroy_all + effect_space (elevators/lifts/passages) + spier (6 sites); elevator floor-menu got elf_idx original-index tracking (like gozone); doormove/elvmove/liftmove resets; verified all .length() sites; (7) [DONE] Transportation — bike/aircraft/vehicle/air_turbulence/airbeacon; KEY FINDING: bikes/aircrafts/vehicles were ALREADY nullable (they self-null on crash/death in normal play, so every cross-cutting consumer — menu/spier/weapon/bullet/glider/tracker/map/effect_space/npc/projectile/bomb/mine/timebomb — already guards; despawn added NO new guard burden for those three, just null+teardown mirroring death); air_turbulences + airbeacons were NEWLY nullable (only resize(0) before) so guarded their every loop: air_turbulence turbcheck + destroy_all + effect_space:63; airbeacon destroy_all + effect_space:141 + aircraft's 3 airbeacon loops (enable at land-gear, topdown_on_beacon, on_valid_beacon); no backing tiles + no removal-sound cue on any (destroy_all silent) so despawn silent; single coords bikex/plainx/vehx (x/y[/z]); air_turbulence corner x/miny(2d) or x/y/minz(3d); airbeacon corner minx/balt(2d), minx/miny(topdown), minx/miny/balt(3d); ride/fly/drive globals (onbike/inplain/invehicle + 9 capability flags) restored ONLY if the removed unit was the active moveable one; normalize_buildtype already maps aircraft_beacon->airbeacon, aircraft_turbulence->air_turbulence; (8) [DONE] Kombat+misc -- npc/projectile/ttsenemie/checkpoint/timed_text (dispatch keys; tts uses on-disk keyword "ttsenemie" via normalize tts_enemie->ttsenemie); TEMPLATE INTENTIONALLY EXCLUDED (tpl has no runtime array -- read_template expands into platforms/staircases/walls via dispatch_entity_line, so those tiles are despawned as platform/wall/staircase; `despawn template` correctly falls through to "nothing to remove"); npc + ttsenemie + checkpoint ALREADY nullable (npc death nulls @1661 + arena reuses null slots; ttsenemie death nulls; checkpoint acquire nulls @107) so consumers pre-guard -- NO new guard burden; PROJECTILE is a COMPACTED array (projmove uses remove_at @67, consumers effect_space/object-menu assume no nulls) so despawn_projectile uses remove_at NOT sentinel-null -- the ONE despawn that removes by compaction; timed_text was never-removed (compacted-by-absence) + soundless so made sentinel-null with a single new guard on timedtxtloop (its only loop; no effect_space/spier refs); despawn_npc mirrors destroy_all_npcs REMOVAL semantics (helper *remove* cue, stop tauntsound/spawnsound/refsound/refsound2/launchsound) NOT kill semantics (no xp/kills/bodyfall/item drop), plus per-npc clean_dead_npc_bullets(target) + tracking_target_died("npc",target); despawn_ttsenemie mirrors destroy_all (stop ttsemsound loop + delete the 3 generated TTS temp files, no *remove* cue); despawn_checkpoint stops checksound+getsound (a checkpoint in the array is never last_checkpoint since acquire removes-as-it-records, so no dangling handle); coords x/y[/z] on npc/ttsenemie(ttsemx/ttsemy/ttsemz)/checkpoint, prox/proy(current pos, moves) on projectile, minx/miny[/minz] on timed_text; (9) [DONE] Docs -- commands.txt got a /despawn entry (alphabetical, between delmap and draw), the compiled-maps section adds despawn to the trusted middle group + unbuild to the editing group, and the /spawn cleanup paragraph was rewritten to point at /despawn (was the false "only /kill/killall, npc-only" note the dev remembered); switches.txt's "target NPCs limited to /killall and /kill" claim updated to include /despawn; changelog 14.8 got a despawn entry at the top (above unbuild, 9 entries total, under the 10 minor cap); todo item moved Unfinished->Finished. DESPAWN FEATURE COMPLETE (ships 14.8). (9) Docs (commands.txt + changelog + update /spawn's "no cleanup verb" note). Each category section adds its cases + proper teardown, independently testable.

**Build sequencing (SETTLED):** build in SEVERAL SECTIONS, ONE COMMAND AT A TIME (do unbuild fully, or despawn fully, before the other — dev's call which first), and DOCUMENT THEM SEPARATELY (separate commands.txt entries, separate changelog entries).

**OPEN details (small, decide at build time):**
- **Feedback behavior** — silent on success (good for switch cleanup) vs a spoken confirmation; and what to say when there's nothing of that type to remove (a notice like "There's no sign to remove." vs silent). Likely: silent success + spoken notice on nothing-to-remove, but undecided.
- **Item-mode siblings** — build maps item_door→door etc.; does unbuild/despawn need the same aliasing?

**Docs when built:** commands.txt entries (unbuild decompiled-only; despawn trusted-on-compiled — note both in the "Commands on compiled maps." section); UPDATE the `/spawn` help paragraph that says only /kill/killall exist for cleanup; changelog. Alphabetize into allcommands + commands.txt. On the todo list this is the "Add some commands to remove objects placed by the build and spawn commands." unfinished item.
