.claude

*.mp3
*.ogg
*.wav

*.map
*.spack

New File*.txt

# Python bytecode cache
__pycache__/

lib/
releases/

# NVGT compile bundle: tools.py runs `nvgt -c` from src/, so the bundle lands at
# src/sf/ before it's moved into releases/. Anchored to src/ so it does NOT match
# the top-level sf/ assets folder.
/src/sf/
/src/sf.exe
